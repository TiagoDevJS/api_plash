import { Router } from "express";

const router = Router()


router.get(
    "/covers",
   
  );
  router.post(
    "/create-event-cover",
    
  );
  router.delete(
    "/delet-event-cover/:slug",
  
  );
  //    

  export default router